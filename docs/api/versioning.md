# API Versioning

NizeCloud APIs will use explicit versioning.

Initial convention:

/api/v1/...

Breaking API changes require a new major API version.

Internal service contracts may evolve independently but must maintain clearly defined boundaries.
